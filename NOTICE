Non-Shannon Inequalities

Copyright 2026 Christopher Boone

A hybrid Lean 4 plus Python research repository for discovering,
classifying, and validating non-Shannon information inequalities.

This repository contains material under multiple licenses.

Project-authored substantive source code is licensed as follows:
  - Lean code (`NonShannon/**/*.lean`, `NonShannonTest/**/*.lean`,
    `NonShannon.lean`, `NonShannonTest.lean`) under the Apache License 2.0.
    See LICENSES/Apache-2.0.txt.
  - Other substantive code (Python under `src/` and `tests/`, the
    `bin/bootstrap-worktree` shell script) under the MIT License.
    See LICENSES/MIT.txt.

Hand-authored infrastructure config and data — Makefile, lakefile.toml,
pyproject.toml, YAML (including CI workflows under `.github/workflows/`),
JSONC, .editorconfig, .gitignore, cspell word list, hand-authored JSON
schemas under `schemas/`, and hand-authored fixture data under
`data/fixtures/` — is licensed under MIT via the `REUSE.toml`
config-group annotation. These files are not content over which the
project asserts substantive authorship; the REUSE.toml glob keeps lint
clean without per-file sidecars or inline headers on every dotfile.

Project-authored prose — READMEs, agent configs, planning documents,
research notes, the bibliography, and this NOTICE — is licensed under
Creative Commons Attribution 4.0 International (CC BY 4.0). See
LICENSES/CC-BY-4.0.txt. This material is covered by the `REUSE.toml`
prose-group annotation; per-file SPDX headers are not required for
prose.

CODE_OF_CONDUCT.md is adapted from the Contributor Covenant and is
licensed under Creative Commons Attribution-ShareAlike 4.0
International (CC BY-SA 4.0). See LICENSES/CC-BY-SA-4.0.txt. Coverage
is authoritative via a dedicated `REUSE.toml` override annotation on
that one file.

Files auto-regenerated by tooling — the Lake manifest
(`lake-manifest.json`), the pinned Lean toolchain (`lean-toolchain`),
and the uv lock file (`uv.lock`) — are dedicated to the public domain
under CC0 1.0. See LICENSES/CC0-1.0.txt. Content is mechanical output
the project does not originate; the CC0 dedication is covered via the
`REUSE.toml` generated-group annotation rather than per-file headers.

Third-party bundled reference material under `references/papers/**`
and `references/transcriptions/**` is reserved for study,
verification, and scholarly context. It is covered under
`LicenseRef-Reference-Material`. See
LICENSES/LicenseRef-Reference-Material.txt. Copyright in those
materials remains with the original authors and publishers, as
identified in the materials themselves. No license to modify,
redistribute, or republish is granted by this repository. Both
directories are currently placeholders; the REUSE.toml
reference-material annotation is pre-wired so that future additions
are covered without further policy work.

The following paths fall outside the authored-license sweep and are
not covered by SPDX tags in this repository:
  - LICENSES/ itself (tool convention)
  - Vendored dependency state (e.g. .lake/packages/**, .venv/,
    __pycache__/, .git/)
  - Build artifacts and caches that are gitignored

Per-file SPDX metadata together with the root REUSE.toml prose,
config, generated, structural, CoC, and reference-material
annotations is authoritative for this repository. This NOTICE is a
summary; it does not override per-file or per-glob tags. See
https://reuse.software/ for the REUSE specification and tooling.
